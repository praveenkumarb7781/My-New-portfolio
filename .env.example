# Server-only — never commit real values. Add the same names in Vercel → Settings → Environment Variables.

GROQ_API_KEY=
OPENROUTER_API_KEY=

# Optional overrides
# GROQ_MODEL=llama-3.3-70b-versatile
# OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
